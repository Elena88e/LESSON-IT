

 Написать программу, которая из имеющегося массива строк
 формирует новый массив из строк,
 длина которых меньше, либо равна 3 символам.

//РЕШЕНИЕ (с использованием двух массивов)

Консоль.Write("Сколько элементов Вы хотите ввести? ");
Преобразовать  = размер  int.ToInt32(консоль.Строка чтения());

строка[] arrayStrings = новая  строка[размер];
для    (int i=0; i  
{
    Консоль.WriteLine($"Введите {i+1}-й элемент: ");
    Преобразовать = строка элемента .toString(консоль.Строка чтения());
    arrayStrings[i] = элемент;
}

строка[] arrayFinal = новая  строка[размер];
3 = len int;
0 =  pos  int;

для    (int j=0; j  
{
    если (arrayStrings[j].Длина <= len)
    {
        arrayFinal[pos] = arrayStrings[j];
        ++поз;
    }
}

Консоль.Строка записи();
printArray(arrayFinal);



// РЕШЕНИЕ 2 (с использованием одного массива)
/* 

Консоль.Write("Сколько элементов Вы хотите ввести? ");
размер int = Преобразовать.ToInt32(консоль.Строка чтения());
строка[] arrayStrings = новая строка[размер];
int len = 3;
int pos = 0;
для (int i = 0; i < size; i++)
{
 Консоль.WriteLine($"Введите {i+1}-й элемент: ");
 строковый элемент = Преобразовать.toString(консоль.Строка чтения());
 если (элемент.Длина <= len)
    {
 arrayStrings[pos] = элемент;
 pos++;
    }
}
Консоль.Строка записи();
printArray(arrayStrings);
*/

// ____Блок вспомогательных методов____
//
//___________________

printArray  void(строка[] массив)
{
    для    (int i=0; i  Длина; i++)
    {
        Консоль.Запись(массив [i] + " ");
    }
    Консоль.Строка записи();
